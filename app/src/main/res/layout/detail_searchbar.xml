<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    >
    <ImageView android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="center"
        android:background="@mipmap/search_bar"
        android:clickable="false"
        />
    <EditText
        android:layout_width="match_parent"
        android:paddingLeft="@dimen/paddingLeftEditTextSearchBar"
        android:paddingRight="@dimen/paddingLeftEditTextSearchBar"
        android:layout_height="match_parent"
        android:inputType="textPersonName"
        android:text=""
        android:clickable="false"
        android:textSize="@dimen/size_text_detail"
        android:hint="Search"
        android:textColorHint="@color/BlackAlpha20"
        android:ems="10"
        android:id="@+id/editText"
        android:ellipsize="end"
        android:background="@android:color/transparent"
        />


    <ImageButton
        android:layout_width="@dimen/heightArrowSearchBar"
        android:layout_height="@dimen/heightArrowSearchBar"
        android:id="@+id/btnArrowSearch"
        android:layout_gravity="right"
        android:clickable="true"
        android:scaleType="fitCenter"
        android:background="@android:color/transparent"
        />






</FrameLayout>